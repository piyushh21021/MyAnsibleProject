---
- name: shutting the nodes
  hosts: all
  become: true
  tasks:
    - name: Power Off
      when: ansible_facts['os_family'] == "RedHat"
      delegate_to: localhost
      azure_rm_virtualmachine:
        resource_group: vm-rg
        name: "{{ inventory_hostname }}"
        started: false
